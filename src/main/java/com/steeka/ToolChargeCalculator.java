package com.steeka;

/**
 * Functional Interface to calculate charges for a given tool
 * Take into account
 * weekday, weekend and holiday charges
 *
 */
public class ToolChargeCalculator {

    public void calculate(String toolCode){
        //specify date range



        //From checkoutDATE to number of days
// check if day weekday, weekend or holiday Calculate holidays
  /*      If( isWeekend(date) && ToolCharge.isWeekendCharge(){
            ChargedDays++:
        } else if isHolday(date) && toolCharge.isHoldayCharged{
            ChargeDays++
        } else{
            ChargeDays++
        }
    }

    TotalChargePre= chargedDays× dailyRentalCharge;
    If(hasDiscount(checkout.getDiscount))
*/

}
}
